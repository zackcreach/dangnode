mixin loginForm()
  form.form(action='/login' method='POST')
    h2 Login
    label(for="email")
    input(type="email")
    input(type="password")
    input.button(type="submit" value="Log In →")
  